# Copy outside the repository and point to a fresh restore-only password file.
WORKLEDGER_RESTORE_POSTGRES_PASSWORD_FILE=/etc/workledger/restore/postgres_password
WORKLEDGER_RESTORE_DATABASE=workledger_restore
WORKLEDGER_RESTORE_USER=workledger_restore

