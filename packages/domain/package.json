{
  "name": "@workledger/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Private WorkLedger framework-independent domain boundary.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.json --pretty false",
    "typecheck": "tsc --build tsconfig.json --pretty false"
  },
  "devDependencies": {
    "@workledger/config": "workspace:*"
  }
}
